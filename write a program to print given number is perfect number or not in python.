n=int(input())
    sum of divisors = 0
    for i in range(1, n):
        if number % i == 0:
            sum of divisors += i

    return sum of divisors == n
if is_perfect_number(n):
    print(f"{n} is a perfect number.")
else:
    print(f"{n}is not a perfect number.")
